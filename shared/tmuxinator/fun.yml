---
# Personal projects (separate from deepreel work in /workspace/core/).
# Repos here are cloned by bootstrap from the fun_repo_urls tfvar.
#
# Start with:  tx fun

name: fun
root: /workspace/fun

startup_window: sandbox

windows:
  # Root shell — credential mgmt (sudo sync-secrets, etc.)
  - admin:
      panes:
        - sudo -i

  # Claude Code on the sandbox infra repo
  - sandbox:
      root: /workspace/fun/sandbox
      panes:
        - claude
